import React from "react";

import ContactCard from "./ContactCard";

const Contact = () => {
  return (
    <section id="contact">
      <ContactCard />
    </section>
  );
};

export default Contact;
